package Testy;


public class PrikazJdiTest {
    /*private Hra hra;
    private Lokace les;
    private Lokace mytina;
    private PrikazJdi prikazJdi;

    @BeforeEach
    public void setUp() {
        les = new Lokace("Les", "Hustý temný les");
        mytina = new Lokace("Mýtina", "Malá mýtina uprostřed lesa");

        les.pridejVychod(mytina);
        hra.setAktualniLokace(les);

        prikazJdi = new PrikazJdi(svet);
    }

    @Test
    public void testProvedBezParametru() {
        String vysledek = prikazJdi.proved();
        assertEquals("Kam mám jít? Musíš zadat jméno lokace.", vysledek);
    }

    @Test
    public void testProvedNeznamySmer() {
        String vysledek = prikazJdi.proved("neznámo");
        assertEquals("Tam se jít nedá!", vysledek);
    }

    @Test
    public void testProvedPlatnySmer() {
        String vysledek = prikazJdi.proved("Mýtina");
        assertEquals("Malá mýtina uprostřed lesa", vysledek);
        assertEquals(mytina, svet.getAktualniLokace());
    }

    @Test
    public void testProvedOpetovne() {
        prikazJdi.proved("Mýtina");
        String vysledek = prikazJdi.proved("Les");
        assertEquals("Hustý temný les", vysledek);
        assertEquals(les, svet.getAktualniLokace());
    }*/
}

